<%# locals: slide: %>
<div class="max-w-3xl">
  <p class="mb-3 text-sm font-medium text-gray-500">Showcase overlay — desktop (sm+)</p>
  <%= render 'fictions/showcase_slide_overlay_component_preview/slide_frame',
             slide: slide,
             frame_class: 'h-[calc(24rem+1em)] max-w-3xl' %>
</div>
