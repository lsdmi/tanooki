<% page_gutter = 'mx-5 md:mx-6 lg:mx-4' %>

<div class="relative z-10">
  <section class="mx-auto xl:w-[1300px]">
    <div class="<%= page_gutter %> mt-8 overflow-hidden rounded-xl border border-gray-200 shadow-md lg:mt-10 lg:rounded-2xl dark:border-gray-700">
      <%= render 'root/hero' %>
    </div>

    <div class="<%= page_gutter %>">
      <div class="mb-8 mt-8 lg:mb-14 lg:mt-14">
        <header class="mb-8 flex items-stretch gap-3 sm:gap-4">
          <div class="w-1.5 shrink-0 rounded-lg bg-cyan-700 dark:bg-rose-500" aria-hidden="true"></div>
          <div class="flex min-w-0 flex-1 items-center justify-between gap-4">
            <h2 class="text-xl lg:text-2xl font-bold leading-snug tracking-tight text-stone-900 dark:text-slate-100">
              Популярне <span class="text-cyan-700 dark:text-rose-400">ранобе</span>
            </h2>
            <%= render 'root/section_more_link',
                       path: fictions_path,
                       link_class: 'text-cyan-700 dark:text-rose-400' %>
          </div>
        </header>

        <%= render 'root/fictions', fictions: @top_fictions.limit(10) %>
      </div>

      <% if @recently_updated_fictions.any? %>
        <%= render 'root/recently_updated',
                   fictions: @recently_updated_fictions,
                   latest_chapters_by_fiction: @recently_updated_chapters %>
      <% end %>

      <%= render 'shared/adsense_home_banners' %>
    </div>
  </section>

  <%= render 'root/tales', tales: @tales, top_tale: @top_tale %>

  <section class="mx-auto xl:w-[1300px]">
    <div class="<%= page_gutter %>">
      <%= render 'root/videos', videos: @videos, video_tag_counts: @video_tag_counts %>
    </div>
  </section>
</div>
