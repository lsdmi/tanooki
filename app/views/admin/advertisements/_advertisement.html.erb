<%= link_to advertisement.resource, class: "relative overflow-hidden h-auto block", aria: { label: advertisement.cover.blob.filename } do %>
  <%= video_tag url_for(advertisement.cover),
                alt: advertisement.cover.blob.filename.to_s,
                autoplay: true,
                class: 'top-0 left-0 w-full h-full',
                loop: true,
                muted: true,
                playsinline: true,
                poster: url_for(advertisement.poster) %>
<% end %>
