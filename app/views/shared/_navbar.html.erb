<nav class="bg-gray-800">
  <div class="mx-auto max-w-7xl px-2 sm:px-6 lg:px-8">
    <div class="relative flex h-16 items-center justify-between">
      <div class="absolute inset-y-0 left-0 flex items-center sm:hidden">
        <!-- Mobile menu button-->
        <%= render "shared/mobile_menu_button" %>
      </div>
      <div class="flex flex-1 items-center justify-center sm:items-stretch sm:justify-start">
        <div class="flex flex-shrink-0 items-center">
          <img class="hidden h-8 w-auto lg:block" src="https://tailwindui.com/img/logos/mark.svg?color=indigo&shade=500" alt="Your Company">
        </div>
        <div class="hidden sm:ml-6 sm:block">
          <div class="flex space-x-4">
            <%= link_to 'Головна', root_path, class: 'hover:bg-gray-900 text-white px-3 py-2 rounded-md text-sm font-medium', area: { current: 'page' } %>
          </div>
        </div>
      </div>
      <%= render "shared/search" %>
      <% if current_user %>
        <%= render 'shared/user_dropdown' %>
      <% else %>
        <div class="hidden sm:ml-6 sm:block">
          <div class="flex space-x-4">
            <%= link_to 'Увійти', new_user_session_path, class: 'text-white px-3 py-2 rounded-md text-sm font-medium', area: { current: 'page' } %>
          </div>
        </div>
      <% end %>
    </div>
  </div>
</nav>