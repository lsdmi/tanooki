<% if user_signed_in? %>
  <%= render "shared/chat_widget_shell", shell_id: "chat-widget-auth", signed_in: true %>
<% else %>
  <%= render "shared/chat_widget_shell", shell_id: "chat-widget-guest", signed_in: false %>
<% end %>
