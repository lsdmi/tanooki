<%# locals: placement: (:top|:bottom) %>
<% return unless chapter_reader_ad_live?(placement) %>

<% wrapper_classes = [
  'reader-ad-slot reader-ad-slot--compact reader-ad-slot--loading relative overflow-hidden rounded-md sm:rounded-lg',
  (placement == :bottom ? 'reader-ad-slot--bottom my-2 sm:my-4 md:my-6' : 'reader-ad-slot--top my-2 sm:my-4 md:my-6')
].join(' ') %>

<div class="<%= wrapper_classes %>"
     data-controller="adsense-unit"
     data-adsense-unit-live-value="true"
     data-adsense-unit-placement-value="<%= placement %>">
  <ins class="adsbygoogle block w-full"
       style="display:block"
       data-ad-client="<%= adsense_client_id %>"
       data-ad-slot="<%= chapter_reader_ad_slot(placement) %>"
       data-ad-format="auto"
       data-full-width-responsive="true"></ins>
</div>
