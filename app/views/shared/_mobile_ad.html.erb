<%= link_to advertisement.resource, class: 'sm:hidden relative overflow-hidden h-0', aria: { label: advertisement.cover.blob.filename } do %>
  <%= image_tag url_for(advertisement.poster), class: 'mx-auto mb-4 w-[96%]' %>
<% end if advertisement.present? %>
