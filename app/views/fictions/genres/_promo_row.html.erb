<%# locals: (wrapper_class: "mb-10 lg:mb-14") %>
<section class="<%= wrapper_class %>" aria-label="Писальня та запит на переклад">
  <div class="flex flex-col md:flex-row gap-5 md:gap-6">
    <div class="w-full md:flex-1 lg:flex-[2] min-w-0">
      <%= render 'root/prefooter/promo_card', **fiction_genre_promo_card %>
    </div>
    <div class="w-full md:flex-1 lg:flex-1 min-w-0">
      <%= render 'fictions/translation_request_promo' %>
    </div>
  </div>
</section>
