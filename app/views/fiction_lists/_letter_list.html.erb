<% fiction_hash_sorted.keys.each do |letter, _fictions| %>
  <%= button_tag letter, onclick: "location.href='#letter-#{letter}'", class: 'mr-1 sm:mr-2 text-xs sm:text-xl font-light text-gray-500 hover:text-gray-900 transform transition duration-500 hover:scale-125' %>
<% end %>