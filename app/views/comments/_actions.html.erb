<div class="mx-auto mt-6">
  <%= link_to new_comment_path(comment: { parent_id: comment.id, commentable_id: commentable.id, commentable_type: application_record_child(commentable), user_id: current_user.id }), data: { turbo_stream: "" }, class: "flex mt-4 text-sm text-gray-900 hover:underline font-medium #{'dark:text-white' if comments_dark_mode?}" do %>
    Відповісти
  <% end %>
</div>