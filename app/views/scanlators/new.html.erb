<section class="bg-white z-10 relative">
  <div class="py-8 px-4 sm:px-6 md:px-12 lg:px-24 mx-auto lg:py-8" data-controller="dropzone">
    <%= simple_form_for @scanlator, as: :scanlator, url: scanlators_path, method: :post, html: { class: "form" } do |f| %>
      <%= render 'form', scanlator: @scanlator, f: f %>
    <% end %>
  </div>
</section>
